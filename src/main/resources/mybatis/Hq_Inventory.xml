<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.Hq_InventoryMapper">
    <select id="hq_InventorySelect" parameterType="int" resultType="Hq_InventoryDto">
        select hq_inventory_id, batch_id, hq_quantity from hq_inventory
        where batch_id = #{batch_id}
        order by hq_inventory_id asc
    </select>
    <update id="hq_InventoryUpdate" parameterType="Hq_InventoryDto">
        update hq_inventory set hq_quantity = #{hq_quantity}
        where hq_inventory_id = #{hq_inventory_id}
    </update>

</mapper>